definitions:

  User:
    type: object
    properties:
      id:
        type: integer
        format: int32
      firstName:
        type: string
      lastName:
        type: string
      email:
        type: string
      phone:
        type: string
      password:
        type: string
      address:
        type: string
#      appointments:
#        type: array
#        items:
#          $ref: '#/definitions/Appointment'

  Appointment:
    type: object
    properties:
      labId:
        type: integer
        format: int32
      timing:
        type: string
      date:
        type: string
      status:
        type: string
      labTest:
        type: array
        items:
          $ref: '#/definitions/LabTest'

  LabTest:
    type: object
    properties:
      id:
        type: integer
        format: int32
      name:
        type: string
      specialRequirements:
        type: string

  City:
    type: object
    properties:
      id:
        type: integer
        format: int32
      name:
        type: string

  ArrayOfCities:
    type: array
    items:
      $ref: '#/definitions/City'
